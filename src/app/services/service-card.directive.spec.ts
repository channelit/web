import { ServiceCardDirective } from './service-card.directive';

describe('ServiceCardDirective', () => {
  it('should create an instance', () => {
    const directive = new ServiceCardDirective();
    expect(directive).toBeTruthy();
  });
});
